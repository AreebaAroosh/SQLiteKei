<UserControl x:Name="Control" x:Class="SQLiteKei.UserControls.TableRecordsTabContent"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SQLiteKei.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="20"></RowDefinition>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <Button x:Name="SelectButton" Content="Select" Width="50" HorizontalAlignment="Left" Click="ExecuteSelect" />
        </StackPanel>
        <DataGrid x:Name="RecordsDataGrid" Grid.Row="1" HeadersVisibility="Column">
        </DataGrid>
        <TextBox x:Name="StatusBar" Grid.Row="2" Style="{StaticResource SelectableStatusBar}"
                 VerticalAlignment="Center"/>
    </Grid>
</UserControl>
